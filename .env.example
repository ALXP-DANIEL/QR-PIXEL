# Public canonical app URL used for metadata, Open Graph, and absolute share links.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: render the maintenance screen instead of the QR generator.
QR_PIXEL_MAINTENANCE=false

# Optional: show the debug HUD in production builds without pressing Ctrl+D.
NEXT_PUBLIC_QR_PIXEL_DEBUG=false
